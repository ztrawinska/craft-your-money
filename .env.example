# Copy this file to `.env.local` (gitignored) and fill in your key, then restart
# the dev server. With a key present, Price Check is written live by Claude;
# without it, Price Check falls back to the built-in scripted review.
#
# Get a key: https://console.anthropic.com/  →  Settings → API Keys
ANTHROPIC_API_KEY=
